@import './../var.styl'
.ml-checkbox-item {
  font-size: $font-size;
  padding-bottom: 5px;
  padding-top: 5px;
  .ml-checkbox-scale-enter {
    opacity: 0;
    transform: scale(0.4);
  }
  .ml-checkbox-scale-leave-active {
    opacity: 0;
    transform: scale(0.4);
  }
  .ml-checkbox-label {
    padding-right: 5px;
    cursor: pointer;
    margin-left: 28px;
    user-select: none;
    min-height: 22px;
    line-height: 22px;
    word-break: break-all;
    word-break: break-word;
  }
  .ml-checkbox-icon {
    width: 22px;
    height: 22px;
    float: left;
    position: relative;
    cursor: pointer;
    .ml-checkbox-on {
      transition: $scale-fade-transition;
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: 2;
      i {
        color: $color-primary;
      }
    }
    .ml-checkbox-on.is-disabled {
      i {
        color: $color-aaa;
        opacity: .4;
      }
    }
    .ml-checkbox-no.is-disabled {
      background-color: $color-aaa
      opacity: .4;
      border-radius: $border-radius;
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: 1;
      overflow: hidden;
      i {
        display: none;
      }
    }
    i {
      font-size: 22px;
      color: $color-aaa;
      line-height: 22px;
    }
  }
}